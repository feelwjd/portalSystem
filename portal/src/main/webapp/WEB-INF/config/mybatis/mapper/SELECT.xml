<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="portal.dao.MySqlDao"> <!-- Repository와 연동을 위해 사용될 namespace -->
	<select id="selectMessageBackbon" resultType="List">
		SELECT
			*
		FROM 
			messageBackbon
	</select>
</mapper>
